spring:
  application:
    name: resource-service
  profiles:
    active: dev
  cloud:
      config:
          uri: http://configserver:8071


logging:
  level:
    org:
      hibernate: INFO
      SQL: DEBUG
      cache: DEBUG
      stat: DEBUG

song-service:
  path: /api/v1/songs