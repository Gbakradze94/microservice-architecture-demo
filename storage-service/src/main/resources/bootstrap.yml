spring:
  application:
    name: storage-service
  cloud:
    config:
      uri: http://configserver:8071
  security:
    oauth2:
      resource:
        user-info-uri: http://localhost:9000/api/auth/user